/* eslint-disable react/prop-types */
import React from 'react';

const ChatHeaderFunctionComponent = ({ user }) => (
  <h1>{`${user.name} - Chat Room`}</h1>
);

export default ChatHeaderFunctionComponent;
