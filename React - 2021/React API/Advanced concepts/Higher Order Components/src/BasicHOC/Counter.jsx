/* eslint-disable react/prop-types */
import React from 'react';

const Counter = ({ data }) => (
  <div className="counter">
    <h1>Counter</h1>
    <p>{data}</p>
  </div>
);

export default Counter;
